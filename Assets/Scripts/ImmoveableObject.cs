using UnityEngine;

namespace SpaceCape {
    public class ImmoveableObject : MonoBehaviour {
        // Start is called before the first frame update
        protected
        // Start is called before the first frame update
        void Start() {

        }

        // Update is called once per frame
        protected

        // Update is called once per frame
        void Update() {

        }
    }
}